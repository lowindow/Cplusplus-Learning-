## constexpr 关键字

让变量/函数/类在编译期确定值

与const区别 ：const只强调只读，值可能在运行期确定，constexpr强调编译期常量，值必须在编译阶段计算出来

作用:

1.constexpr修饰变量 编译器强制要求变量的值在编译期确定，可用于数组大小、模板参数等

2.constexpr修饰函数，函数参数若是编译期常量，则函数会在编译期执行，返回常量，否则退化普通函数

3.constexpr修饰类，类的构造参数为编译期常量，则在编译期创建类，提高效率，更强安全检查 



static_assert 编译期断言